
name: p4vfs-verify

on:
  workflow_call:
  workflow_dispatch:
  push:
    branches:
      - 'main'
      - 'dev/*'
  pull_request:
    branches:
      - 'main'
      - 'dev/*'

permissions:
  contents: read
  
jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [windows-latest]
        configuration: [Debug, Release]

    steps:
    - id: 'Build'
      uses: ./.github/actions/p4vfs-build
      with:
        configuration: '${{matrix.configuration}}'
